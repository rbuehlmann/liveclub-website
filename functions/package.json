{
  "name": "liveclub-functions",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^6.9.0",
    "sharp": "^0.35.3",
    "stripe": "^22.4.0"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "@types/nodemailer": "^6.4.0",
    "typescript": "^5.6.0"
  }
}
